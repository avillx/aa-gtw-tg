[project]
name = "myapi-client"
version = "1.2.0"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.0",
    "pytelegrambotapi>=4.36"
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]

[tool.mypy]
strict = true
